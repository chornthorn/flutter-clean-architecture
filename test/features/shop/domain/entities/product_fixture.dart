import 'package:flutter_x/features/shop/domain/entities/product.dart';

// The canonical product tests build on, so ids stay consistent across files.
const product = Product(id: 'sku-42', name: 'Espresso cup', price: 12.5);
